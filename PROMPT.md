@AGENTS.md

# Beads Otto Loop

- Run `bd prime` then `bd ready` and pick ONE issue
- `bd update <id> --status=in_progress` then implement that issue only
- Create discovered issues with `bd create`, never TODO comments
- When done with that ONE issue: Land the plane
